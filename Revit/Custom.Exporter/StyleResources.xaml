<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Color x:Key="CustomExporterWhite">#F7F7F7</Color>
    <Color x:Key="CustomExporterBlue">#3B82F6</Color>
    <Color x:Key="CustomExporterGreen">#22C55E</Color>
    <Color x:Key="CustomExporterYellow">#F59E0B</Color>
    <Color x:Key="CustomExporterNavy">#3730A3</Color>
    <Color x:Key="CustomExporterDarkGray">#374151</Color>
    <Color x:Key="CustomExporterMediumGray">#6B7280</Color>
    <Color x:Key="CustomExporterGray">#D1D5DB</Color>
    <Color x:Key="CustomExporterLightGray">#F3F4F6</Color>
    <Color x:Key="CustomExporterDisabledGray">#9CA3AF</Color>
    <Color x:Key="CustomExporterRed">#EF4444</Color>
    <Color x:Key="CustomExporterWarning">#FEF3C7</Color>
    <Color x:Key="CustomExporterWarningSecondary">#F97316</Color>

    <SolidColorBrush x:Key="CustomExporterWhiteBrush" Color="{StaticResource CustomExporterWhite}" />
    <SolidColorBrush x:Key="CustomExporterBlueBrush" Color="{StaticResource CustomExporterBlue}" />
    <SolidColorBrush x:Key="CustomExporterGreenBrush" Color="{StaticResource CustomExporterGreen}" />
    <SolidColorBrush x:Key="CustomExporterYellowBrush" Color="{StaticResource CustomExporterYellow}" />
    <SolidColorBrush x:Key="CustomExporterNavyBrush" Color="{StaticResource CustomExporterNavy}" />
    <SolidColorBrush x:Key="CustomExporterDarkGrayBrush" Color="{StaticResource CustomExporterDarkGray}" />
    <SolidColorBrush x:Key="CustomExporterMediumGrayBrush" Color="{StaticResource CustomExporterMediumGray}" />
    <SolidColorBrush x:Key="CustomExporterGrayBrush" Color="{StaticResource CustomExporterGray}" />
    <SolidColorBrush x:Key="CustomExporterLightGrayBrush" Color="{StaticResource CustomExporterLightGray}" />
    <SolidColorBrush x:Key="CustomExporterDisabledGrayBrush" Color="{StaticResource CustomExporterDisabledGray}" />
    <SolidColorBrush x:Key="CustomExporterRedBrush" Color="{StaticResource CustomExporterRed}" />
    <SolidColorBrush x:Key="CustomExporterWarningBrush" Color="{StaticResource CustomExporterWarning}" />
    <SolidColorBrush x:Key="CustomExporterWarningSecondaryBrush" Color="{StaticResource CustomExporterWarningSecondary}" />

    <LinearGradientBrush x:Key="CustomExporterGradientBrush" StartPoint="0,0" EndPoint="1,1.1">
        <GradientStop Offset="0" Color="{StaticResource CustomExporterBlue}" />
        <GradientStop Offset="0.76" Color="{StaticResource CustomExporterGreen}" />
        <GradientStop Offset="1" Color="{StaticResource CustomExporterYellow}" />
    </LinearGradientBrush>

    <DropShadowEffect
        x:Key="CustomExporterDropShadow"
        BlurRadius="10"
        Opacity="0.2" />

    <Style x:Key="BorderInfoStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Background" Value="#fffcf6" />
        <Setter Property="BorderBrush" Value="{StaticResource CustomExporterBlueBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="BorderErrorStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Background" Value="#fffcf6" />
        <Setter Property="BorderBrush" Value="{StaticResource CustomExporterRedBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <!--  Text Blocks  -->

    <Style x:Key="TextBlockBaseStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource CustomExporterDarkGrayBrush}" />
        <Setter Property="FontFamily" Value="Resources/Roboto/#Roboto" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style
        x:Key="TextBlockHeavyStyle"
        BasedOn="{StaticResource TextBlockBaseStyle}"
        TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Resources/Roboto/#Roboto Black" />
    </Style>

    <Style
        x:Key="TextBlockWarningStyle"
        BasedOn="{StaticResource TextBlockBaseStyle}"
        TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Resources/Roboto/#Roboto Regular" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterWarningSecondaryBrush}" />
    </Style>

    <Style x:Key="TextBlockSegoe" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style
        x:Key="TextBlockSegoeCheck"
        BasedOn="{StaticResource TextBlockSegoe}"
        TargetType="TextBlock">
        <Setter Property="Text" Value="" />
    </Style>

    <Style
        x:Key="TextBlockSegoeError"
        BasedOn="{StaticResource TextBlockSegoe}"
        TargetType="TextBlock">
        <Setter Property="Text" Value="" />
    </Style>

    <!--  Text boxes  -->

    <Style x:Key="TextBoxStyle" TargetType="TextBox">
        <Setter Property="Height" Value="30" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{StaticResource CustomExporterMediumGrayBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterDarkGrayBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="0,8,0,4" />
        <Setter Property="FontFamily" Value="/Vim.Revit.ExporterUI;component/Resources/Roboto/#Roboto Medium" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style
        x:Key="ClickableBoxStyle"
        BasedOn="{StaticResource TextBoxStyle}"
        TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style
        x:Key="SelectableTextBoxStyle"
        BasedOn="{StaticResource TextBoxStyle}"
        TargetType="TextBox">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style
        x:Key="ErrorTextBoxStyle"
        BasedOn="{StaticResource SelectableTextBoxStyle}"
        TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource CustomExporterRedBrush}" />
    </Style>
    <Style x:Key="ErrorHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterRedBrush}" />
        <Setter Property="FontFamily" Value="/Vim.Revit.ExporterUI;component/Resources/Roboto/#Roboto" />
    </Style>

    <!--  Buttons  -->

    <Style x:Key="ButtonPrimaryStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource CustomExporterBlueBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="Resources/Roboto/#Roboto Black" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Border.CornerRadius" Value="3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource CustomExporterGrayBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterMediumGrayBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
                <Setter Property="Button.Effect">
                    <Setter.Value>
                        <DropShadowEffect
                            BlurRadius="20"
                            Direction="315"
                            Opacity="0.2" />
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
                <Setter Property="Button.Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="ButtonSecondaryStyle"
        BasedOn="{StaticResource ButtonPrimaryStyle}"
        TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="BorderBrush" Value="{StaticResource CustomExporterBlueBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{StaticResource CustomExporterDisabledGrayBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterDisabledGrayBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Background" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
                <Setter Property="Button.Effect">
                    <Setter.Value>
                        <DropShadowEffect
                            BlurRadius="20"
                            Direction="315"
                            Opacity="0.2" />
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Background" Value="{StaticResource CustomExporterWhiteBrush}" />
                <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
                <Setter Property="Button.Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SegoeButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterMediumGrayBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="OuterBorder"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentControl
                            x:Name="InnerContent"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontSize="{TemplateBinding FontSize}"
                            Foreground="{TemplateBinding Foreground}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource CustomExporterGrayBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource CustomExporterBlueBrush}" />
                <Setter Property="Cursor" Value="Hand" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="FolderSegoeButtonStyle"
        BasedOn="{StaticResource SegoeButtonStyle}"
        TargetType="Button">
        <Setter Property="Content" Value="" />
    </Style>


    <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource CustomExporterDarkGrayBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="5,5,0,5" />
        <Setter Property="FontFamily" Value="/Vim.Revit.ExporterUI;component/Resources/Roboto/#Roboto Medium" />
    </Style>

    <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#A4A4A4" />
        <Setter Property="Foreground" Value="#A4A4A4" />
        <Setter Property="Background" Value="White" />
        <!--<Setter Property="VerticalAlignment" Value="Top"/>-->
    </Style>

    <!--
        Context Menu
        See: https://docs.microsoft.com/en-us/dotnet/framework/wpf/controls/contextmenu-styles-and-templates
    -->
    <Style x:Key="ContextMenuStyle" TargetType="{x:Type ContextMenu}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Grid.IsSharedSizeScope" Value="true" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border
                        x:Name="Border"
                        Background="{StaticResource CustomExporterWhiteBrush}"
                        BorderThickness="0">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasDropShadow" Value="true">
                            <Setter TargetName="Border" Property="Padding" Value="0,5,0,5" />
                            <Setter TargetName="Border" Property="Margin" Value="5,5,10,10" />
                            <Setter TargetName="Border" Property="CornerRadius" Value="4" />
                            <Setter TargetName="Border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="10" Opacity="0.2" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{StaticResource CustomExporterDarkGrayBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="Resources/Roboto/#Roboto Regular" />
        <Setter Property="Padding" Value="0,5,0,5" />
        <Setter Property="IsCheckable" Value="False" />
        <Setter Property="BorderThickness" Value="4,0,0,0" />
        <!--  Set the left border thickness to 5 (it will be colored blue on mouseover)  -->
        <Setter Property="Template">
            <Setter.Value>
                <!--  Custom menu item control template to remove the icon row and to set the left border thickness.  -->
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border
                        x:Name="templateRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <Grid Margin="-1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition
                                    Width="0"
                                    MinWidth="0"
                                    SharedSizeGroup="MenuItemIconColumnGroup" />
                                <!--  Set the icon column to 0 width  -->
                                <ColumnDefinition Width="13" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="30" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumnGroup" />
                                <ColumnDefinition Width="20" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter
                                x:Name="Icon"
                                Width="16"
                                Height="16"
                                Margin="3"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Icon}"
                                ContentSource="Icon"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <Border
                                x:Name="GlyphPanel"
                                Width="22"
                                Height="22"
                                Margin="-1,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Background="#3D26A0DA"
                                BorderBrush="#FF26A0DA"
                                BorderThickness="1"
                                ClipToBounds="False"
                                Visibility="Hidden">
                                <Path
                                    x:Name="Glyph"
                                    Width="10"
                                    Height="11"
                                    Data="F1M10,1.2L4.7,9.1 4.5,9.1 0,5.2 1.3,3.5 4.3,6.1 8.3,0 10,1.2z"
                                    Fill="#FF212121"
                                    FlowDirection="LeftToRight" />
                            </Border>
                            <ContentPresenter
                                x:Name="menuHeaderContainer"
                                Grid.Column="2"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentSource="Header"
                                ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <TextBlock
                                x:Name="menuGestureText"
                                Grid.Column="4"
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                Opacity="0.7"
                                Text="{TemplateBinding InputGestureText}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource CustomExporterNavyBrush}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="#F6F6F6" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource CustomExporterNavyBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="templateRoot" Property="TextElement.Foreground" Value="#FF707070" />
                            <Setter TargetName="Glyph" Property="Fill" Value="#FF707070" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsHighlighted" Value="True" />
                                <Condition Property="IsEnabled" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="templateRoot" Property="Background" Value="#0A000000" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="#21000000" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
